# Run package bins (tsc has a node shebang) with bun itself: no node on the machine.
[run]
bun = true

# All suites live under tests/ (skill tests in tests/<skill>/), outside the shipped skill folders.
[test]
root = "tests"
